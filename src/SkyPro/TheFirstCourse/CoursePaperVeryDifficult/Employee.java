package SkyPro.TheFirstCourse.CoursePaperVeryDifficult;

 class Employee {
   /*  private String companyEmployeeName; // Имя сотрудника
    private int Department; // Отдел
    private float Salary; // Зарплата
    private static int counter = 1; // Счетчик
    int id = 1; // Индификатор

     Employee(String companyEmployeeName, int Department, float Salary) {
        this.companyEmployeeName = companyEmployeeName;
        this.Department = Department;
        this.Salary = Salary;
        this.id = counter++;
    }

     String getCompanyEmployeeName() {
        return this.companyEmployeeName;
    }

     int getDepartment() {
        return this.Department;
    }

     float getSalary() {
        return this.Salary;
    }

     int getId() {
        return this.id;
    }

     void setDepartment(int department) {
        this.Department = department;
    }

     void setSalary(float salary) {
        this.Salary = salary;
    }

     String toString() {
        String var10000 = this.companyEmployeeName;
        return "Сотрудник - " + var10000 + ", Отдел - " + this.Department + ", Зарплата - " + String.format("%.2f", this.Salary) + " ₽ , id=" + this.id;
    }

     String toStringNoDepartment() {
        String var10000 = this.companyEmployeeName;
        return "Сотрудник - " + var10000 + ", Зарплата - " + String.format("%.2f", this.Salary) + " ₽ , id=" + this.id;
    }

    */
}
